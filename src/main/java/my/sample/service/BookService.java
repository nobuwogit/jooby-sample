package my.sample.service;

import org.jooq.DSLContext;
import org.jooq.example.gradle.db.public_.tables.Book;
import org.jooq.impl.DSL;

import static org.jooq.example.gradle.db.public_.Tables.BOOK;

/**
 * @author m-washio
 */
public class BookService {
//    public static Book searchOne(){
//         try (DSLContext ctx = require(DSLContext.class)) {
//       return ctx.transactionResult(conf -> {
//        DSLContext trx = DSL.using(conf);
//        return trx.selectFrom(BOOK)
//            .where(BOOK.ID.eq(1))
//            .fetchOne()
//            .into(Book.class
//      });
//    }
}
